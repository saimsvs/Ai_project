services:
  - type: web
    name: solar-optimizer-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --timeout 300 --workers 1
    envVars:
      - key: PYTHONUTF8
        value: "1"
